# iomha

`iomha` is an early Rust + Iced prototype for a Photoshop/Illustrator-style editor.

Current MVP:

- brush drawing on a canvas
- rectangle and ellipse tools
- layer list with visibility toggles
- stroke/fill palette and stroke-width controls

Run it with:

```bash
cargo run
```
